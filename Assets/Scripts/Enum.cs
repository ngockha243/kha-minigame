
// Direct of character
public enum Direct
{
    Left = 0,
    Right,
}
// All animation of player
public enum AnimatorPlayer
{
    idle = 0,
    move,
    fall,
}
public enum SoundType
{
    click = 0,
    jump,
    collect_diamond,
    lose,
}